name: 文献周报推送

on:
  schedule:
    # 每周日北京时间 20:00 (UTC+8) → UTC 12:00
    - cron: "0 12 * * 0"
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install system deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libcurl4-openssl-dev 2>/dev/null || true

      - name: Install Python deps
        run: |
          pip install -r requirements.txt
          pip install playwright
          playwright install chromium --with-deps

      - name: Scrape (7 days)
        run: python scraper.py
        env:
          SCRAPE_DAYS: "7"
        timeout-minutes: 10

      - name: LLM weekly synthesis
        run: python analyze.py --mode weekly
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        timeout-minutes: 8

      - name: Sync to Supabase
        run: python sync_to_supabase.py weekly
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}

      - name: Generate weekly report
        run: python report_generator.py --mode weekly

      - name: Generate Obsidian notes
        run: python obsidian_notes.py

      - name: Commit Obsidian notes
        continue-on-error: true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git add -f obsidian/ report_weekly.md scraped_papers.json analysis_weekly.json
          git diff --staged --quiet || git commit -m "auto: weekly report W$(date +%V)"
          git push

      - name: Send email
        run: python send_email.py --mode weekly
        env:
          EMAIL_SMTP_HOST: ${{ secrets.EMAIL_SMTP_HOST }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVER: ${{ secrets.EMAIL_RECEIVER }}

      - name: Push to Feishu
        run: python feishu_push.py --mode weekly
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}

      - name: Upload report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: weekly-report
          path: report_weekly.md
          retention-days: 90
